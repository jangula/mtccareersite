import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import './globals.css'

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
})

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
})

export const metadata: Metadata = {
  title: {
    default: 'MTC Careers - Join Our Team',
    template: '%s | MTC Careers',
  },
  description: 'Explore career opportunities at Mobile Telecommunications Company (MTC) Namibia. Join our team and be part of Namibia\'s digital future.',
  keywords: ['MTC', 'careers', 'jobs', 'Namibia', 'telecommunications', 'employment'],
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased min-h-screen flex flex-col`}>
        {children}
      </body>
    </html>
  )
}
